[project]
name = "Asteroids"
version = "0.1.0"
description = "Python version of the classic Asteroids arcade game"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.6.1",
]
